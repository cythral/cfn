version: 0.2
env:
  variables:
    DOTNET_SYSTEM_GLOBALIZATION_INVARIANT: "true"

phases:
  install:
    runtime-versions:
      dotnet: 3.1
    commands:
      - curl -sSL https://dot.net/v1/dotnet-install.sh | bash /dev/stdin --version 2.2.301 --install-dir /root/.dotnet

  build:
    commands:
      - dotnet --info
      - dotnet test -c Release
      - dotnet msbuild tools/package.proj -p:Configuration=Release
      - ./tools/create-networking-config.sh > Networking.config.json

artifacts:
  discard-paths: yes
  files:
    - "Core.template.yml"
    - "Core.config.json"
    - "Utilities.template.yml"
    - "Resources.template.yml"
    - "Dns.template.yml"
    - "Dns.config.json"
    - "Networking.config.json"
    - "Metadata.template.yml"